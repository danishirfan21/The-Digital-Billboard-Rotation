# run_02.py
# Intentional failing attempt to use Qwen model (placeholder).
# This script exits with non-zero status.

import sys

print("Simulating Qwen attempt 2 — this will exit non-zero")
sys.exit(2)
